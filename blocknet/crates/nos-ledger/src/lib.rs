//! BlockNet NOS ledger primitives.
//!
//! This crate will expose the core accounting state machine that keeps
//! track of program budgets, balances, and snapshot roots.

/// Placeholder type so the crate builds as part of the workspace.
pub struct LedgerDraft;

impl LedgerDraft {
    /// Temporary helper demonstrating how initialization APIs may look.
    pub fn new() -> Self {
        Self
    }
}
